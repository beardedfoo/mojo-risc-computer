module firmware (
    input clk,  // clock
    input rst,  // reset
    input address[16],
    output data[16]
  ) {

  always {
    case (address) {
      h0000: data = c{Opcode.ADD, Opcode.REG1, Opcode.REG2, 7h0};
      default: data = 0;
    }
  }
}
